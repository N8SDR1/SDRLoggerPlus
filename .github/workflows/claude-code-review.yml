name: Claude Code Review

on:
  # Use pull_request_target so the review also works for fork PRs.
  # pull_request_target runs in the base-repo context (workflow from main,
  # full permissions, secrets, OIDC available) — unlike `pull_request`,
  # which denies OIDC tokens for fork PRs and breaks claude-code-action.
  pull_request_target:
    types: [opened, synchronize, ready_for_review, reopened]

# Secrets cannot be read from a job- or step-level `if:` — the `secrets` context
# is not available there. Resolving the presence check to a plain string here
# lets the review step skip cleanly (rather than fail) when no credential is
# configured. Note this only tests for presence: a wrong or expired token still
# fails at the API call, which is the honest outcome.
env:
  HAS_CLAUDE_AUTH: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN != '' && 'true' || 'false' }}

jobs:
  claude-review:
    # Optional: Filter by PR author
    # if: |
    #   github.event.pull_request.user.login == 'external-contributor' ||
    #   github.event.pull_request.user.login == 'new-developer' ||
    #   github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR'

    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write

    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-review
        if: env.HAS_CLAUDE_AUTH == 'true'
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          plugin_marketplaces: 'https://github.com/anthropics/claude-code.git'
          plugins: 'code-review@claude-code-plugins'
          prompt: '/code-review:code-review ${{ github.repository }}/pull/${{ github.event.pull_request.number }}'
          # See https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md
          # or https://code.claude.com/docs/en/cli-reference for available options

